# AGENTS.md（Codex 向け・TS Industry サイト制作）

- まずこのフォルダの **`CLAUDE.md`** と `PROJECT_STATE.md` を読む（方針・環境・進捗はそこに集約）。
- このフォルダは独立 git リポジトリ。ワークスペース共通の Codex 運用ルール
  （`codex/*` ブランチ・件名 `[Codex]`・変更申し送り節・触らないもの）は
  `D:\たぶんごみ\ClaudeCODE\AGENTS.md` にあるので、**あわせて読むこと**。
- 変更申し送りはルート `D:\たぶんごみ\ClaudeCODE\CLAUDE.md` の「他エージェントからの変更申し送り」節に
  「web:」を付けて書く。
- 開始手順（fetch → `origin/master` 側の現在地 → 案件メモ → 相手ブランチの `docs/handoff/`）はルート `AGENTS.md` の
  「開始チェックリスト」のとおり。会社・業務の前提は
  `"D:\たぶんごみ\ClaudeCODE\Obsidian\obsidian DATA\Knowledge\会社と業務の基礎.md"`（パスを省略・推測しない）。
  このリポジトリには `HANDOFF.md` はまだ無い（案件が動くとき Claude Code が作る）。
